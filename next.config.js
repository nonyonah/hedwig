/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cryptologos.cc',
      'metadata.ens.domains',
      'api.basename.app',
      'ipfs.io',
      'gateway.ipfs.io'
    ],
  },
}

module.exports = nextConfig